apiVersion: apps/v1
kind: Deployment
metadata:
  name: jenkins-app
  namespace: ivolve
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jenkins-app
  template:
    metadata:
      labels:
        app: jenkins-app
    spec:
      containers:
        - name: jenkins-app
          image: orabi20/jenkins-app-lab34:latest
          ports:
            - containerPort: 8080
      tolerations:
        - key: "workload"
          operator: "Equal"
          value: "app"
          effect: "NoSchedule"
